info:
  name: "[G] User {id}"
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/users/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
