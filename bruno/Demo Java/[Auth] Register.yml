info:
  name: "[Auth] Register"
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8080/v1/auth/register
  body:
    type: json
    data: |-
      {
        "username": "stevie",
        "password": "12345678",
        "name": "Stevie Doe",
        "address": "Da Nang",
        "gender": "MALE",
        "dateOfBirth": "1990-01-15"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
