info:
  name: "[G] Hello"
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8002/v1/hello
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
