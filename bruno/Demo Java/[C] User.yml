info:
  name: "[C] User"
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/v1/user
  body:
    type: json
    data: |-
      
      {
        "username": "jeff",
        "password": "12345678",
        "name": "Jeff Doe",
        "address": "Ho Chi Minh",
        "gender": "OTHER",
        "dateOfBirth": "1995-01-15"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
